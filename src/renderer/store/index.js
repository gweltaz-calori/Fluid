import Vue from "vue";
import Vuex from "vuex";

const state = {};

const mutations = {};

const actions = {};

Vue.use(Vuex);

export default new Vuex.Store({
  state,
  mutations,
  actions,
  strict: process.env.NODE_ENV !== "production"
});
